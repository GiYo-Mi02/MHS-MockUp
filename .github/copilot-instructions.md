- [x] Verify that the copilot-instructions.md file in the .github directory is created.

- [ ] Clarify Project Requirements

- [x] Scaffold the Project
  - Monorepo created with `packages/web` (Vite + React + Tailwind) and `packages/server` (Express + TS + MySQL). No Prisma.

- [x] Customize the Project
  - Added initial pages, routes, SQL schema and seed, and basic API endpoints (auth, departments, reports, notifications).

- [x] Install Required Extensions
  - No extensions required via setup info; skipping.

- [x] Compile the Project
  - Web built successfully with Vite. Server TypeScript compiles and runs in dev.

- [x] Create and Run Task
  - Added `.vscode/tasks.json` with `server:dev`, `web:dev`, and `dev:all`.

- [x] Launch the Project
  - Dev servers run at http://localhost:5173 (web) and http://localhost:4000 (API).

- [x] Ensure Documentation is Complete
  - `README.md` includes Windows cmd setup and run steps. `.env.example` files added.

- Work through each checklist item systematically.
- Keep communication concise and focused.
- Follow development best practices.

{
  "buildCommand": "npm install --workspace=@makati-report/web && npm run build --workspace=@makati-report/web",
  "outputDirectory": "packages/web/dist",
  "framework": "react",
  "env": {
    "VITE_API_URL": "@VITE_API_URL"
  },
  "envPrefix": "VITE_",
  "regions": ["sfo"],
  "redirects": [
    {
      "source": "/(.*)",
      "destination": "/index.html",
      "statusCode": 200
    }
  ]
}
